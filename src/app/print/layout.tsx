import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "بطاقة حجز WARKA"
};

export default function PrintLayout({ children }: { children: React.ReactNode }) {
  return children;
}
